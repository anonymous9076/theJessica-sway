import React from 'react'

const Header = () => {
    return (
        <div
            className='bg-primary tracking-wider text-center py-2 text-white text-lg md:text-xl'
            // style={{ fontFamily: 'var(--next-font-playfair), serif' }}
            style={{ fontFamily: 'var(--next-font-great-vibes), cursive' }}
        >
            “ Your path is yours alone but you don’t have to walk it alone.”
        </div>
    )
}

export default Header;